import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cambodia Exchange Rates | Compare Bank Rates",
  description: "Compare exchange rates from major Cambodian banks including ABA, ACLEDA, Wing, Canadia Bank, and National Bank of Cambodia.",
  keywords: ["Cambodia exchange rates", "KHR USD", "ABA Bank rates", "ACLEDA rates", "Wing Bank", "Cambodia banks"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
